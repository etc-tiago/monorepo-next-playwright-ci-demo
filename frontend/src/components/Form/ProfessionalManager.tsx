
import { FC } from 'react';

import { Title } from '../Title';
import { FormProps } from './types';

const PrManagerForm: FC<FormProps> = ({
}) => (
    <div data-at="pr-manager">
        <Title>Informações básicas</Title>
    </div>
  );


export default PrManagerForm;
